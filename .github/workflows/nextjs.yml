# GitHub Actions workflow for deploying a Next.js site to GitHub Pages
name: Deploy Next.js site to GitHub Pages

on:
  push:
    branches: ["master"] # Replace with your default branch if not 'master'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  # Build job
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: yarn

      - name: Install dependencies
        run: yarn install

      - name: Build Next.js project
        run: yarn build

      - name: Export static files
        run: yarn export
        env:
          NEXT_PUBLIC_BASE_PATH: "/hackaTUM2024" # Set your GitHub Pages base path
        # Ensure the Next.js `next.config.js` has a `basePath` set for GitHub Pages.

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: out # The output directory from `yarn export`

  # Deployment job
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
